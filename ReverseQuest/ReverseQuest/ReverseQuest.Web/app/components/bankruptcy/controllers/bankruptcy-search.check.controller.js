(function () {
    'use strict';

    angular.module('reverseQuestApp.bankruptcy').controller('BankruptcySearchCheckCtrl', BankruptcySearchCheckCtrl);
    BankruptcySearchCheckCtrl.$inject = ['$window', '$filter', '$state', 'notifications', 'session'];

    function BankruptcySearchCheckCtrl($window, $filter, $state, notifications, session) {
        var vm = this;

    }

}());
