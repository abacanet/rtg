(function () {
    'use strict';

    angular.module('reverseQuestApp.bankruptcy').controller('BankruptcySearchDefaultCtrl', BankruptcySearchDefaultCtrl);
    BankruptcySearchDefaultCtrl.$inject = ['$window', '$filter', '$state', 'notifications', 'session'];

    function BankruptcySearchDefaultCtrl($window, $filter, $state, notifications, session) {
        var vm = this;

    }

}());
